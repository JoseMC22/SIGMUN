# Variables de Entorno de EJEMPLO - Backend SIGMUN
# Copia este archivo como .env y completa los valores reales
# NUNCA subas el archivo .env real al repositorio

# ─── Servidor ─────────────────────────────────────────────
PORT=3003

# ─── Base de Datos SQL Server ─────────────────────────────
DB_USER=sa
DB_PASSWORD=Satica2023$$
DB_SERVER=192.168.3.205
DB_DATABASE=Base_sigmun
DB_PORT=1433
DB_ENCRYPT=false

DATABASE_URL="sqlserver://${DB_SERVER}:${DB_PORT};database=${DB_DATABASE};user=${DB_USER};password=${DB_PASSWORD};encrypt=${DB_ENCRYPT}"

# ─── JWT ──────────────────────────────────────────────────
JWT_SECRET=CAMBIA_ESTE_VALOR_POR_UNA_FRASE_SECRETA_LARGA
JWT_EXPIRES_IN=8h

# ─── Frontend / Dev ---------------------------------------------------------
# URL de la aplicación frontend usada para CORS en backend
FRONTEND_URL=http://localhost:3003

# Origen permitido para HMR/dev en la LAN (ajusta a tu IP si accedes desde otra máquina)
DEV_ALLOWED_ORIGIN=http://192.168.3.28:3003
DEV_ALLOWED_ORIGIN_HOST=192.168.3.28

# Entorno de ejecución (development|production)
NODE_ENV=development



# PORT=4000
# #NODE_ENV=development
# NODE_ENV=production

# FRONTEND_URL=http://192.168.3.28:3000
# JWT_SECRET=SisArchivoSatica2024SecretKeyMuyLarga123456
# JWT_EXPIRES_IN=8h

# # ─────────────────────────────────────────────────────────────
# # VARIABLES DE DESARROLLO (PRUEBAS)
# # ─────────────────────────────────────────────────────────────
# DEV_DB_SERVER=192.168.3.205
# DEV_DB_PORT=1433
# DEV_DB_DATABASE=Base_sigmun
# DEV_DB_USER=sa
# DEV_DB_PASSWORD=Satica2023$$
# DEV_DB_ENCRYPT=false
# DEV_DB_TRUST_SERVER_CERTIFICATE=true

# DEV_NAS_PATH=\\192.168.3.100\digital\archivo_prueba\Expedientes
# DEV_NAS_USER=SATICA_NAS\usr_archivo
# DEV_NAS_PASSWORD=Satica2026$

# #net use \\192.168.3.100\digital /user:usr_videos Satica2026$